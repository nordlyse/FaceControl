# Keycloak login-actions POST carries base64 face image (form field). Traefik's default
# forwarding buffer rejects large bodies with 413 unless explicitly raised.
http:
  middlewares:
    keycloak-large-post:
      buffering:
        maxRequestBodyBytes: 67108864
        memRequestBodyBytes: 67108864
